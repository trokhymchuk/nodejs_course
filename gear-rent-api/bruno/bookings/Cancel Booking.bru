meta {
  name: Cancel Booking
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/bookings/{{bookingId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  bookingId: 15
}
