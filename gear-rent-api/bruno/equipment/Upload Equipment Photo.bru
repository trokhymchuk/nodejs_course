meta {
  name: Upload Equipment Photo
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/equipment/{{equipmentId}}/photo
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  photo: @file()
}
